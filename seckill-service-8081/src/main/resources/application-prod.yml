spring:
  config:
    import:
      - nacos:seckill-service.yml
      - nacos:dao.yml?group=EXT_GROUP
      - nacos:redis-config.yml?group=EXT_GROUP
      - nacos:rabbitmq-config.yml?group=EXT_GROUP
  cloud:
    nacos:
      server-addr: nacos:8848
      username: nacos
      password: nacos
      config:
          namespace: 0a3a5563-ecb4-4d2b-af58-455a29ddaaf8
          # 从Nacos读取配置项的超时时间, 单位是 ms，默认值 3000 毫秒
          timeout: 5000
          # 长轮询超时时间
          config-long-poll-timeout: 10000        
          # 轮询的重试时间
          config-retry-time: 2000
          # 长轮询最大重试次数
          max-retry: 3
          # 开启监听和自动刷新
          refresh-enabled: true
      discovery:
          namespace: 0a3a5563-ecb4-4d2b-af58-455a29ddaaf8
